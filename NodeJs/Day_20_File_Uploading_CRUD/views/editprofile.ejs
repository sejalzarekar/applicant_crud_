<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Update Profile</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #f8f9fa;
      padding-top: 40px;
    }

    .container {
      max-width: 600px;
      background: white;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.1);
    }

    h2 {
      text-align: center;
      margin-bottom: 25px;
    }

    .form-control {
      border-radius: 8px;
    }

    .form-label {
      font-weight: 500;
    }

    .profile-preview {
      display: block;
      margin: 15px auto;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }

    .btn-primary {
      width: 100%;
      border-radius: 8px;
      padding: 10px;
      font-weight: 600;
    }
  </style>
</head>
<body>

  <div class="container">
    <h2>Update Profile</h2>

    <form action="/updateprofile" method="post" enctype="multipart/form-data">
      <!-- Hidden Profile ID -->
      <input type="hidden" name="profile_id" value="<%-data.profile_id%>" />

      <!-- Username Field -->
      <div class="mb-3">
        <label class="form-label">Update Username</label>
        <input type="text" class="form-control" name="username" value="<%-data.fullname %>" required />
      </div>

      <!-- File Upload Field -->
      <div class="mb-3">
        <label class="form-label">Update Profile Picture</label>
        <input type="file" class="form-control" name="file" accept="image/*" />
      </div>

      <!-- Preview Current Image -->
      <div class="text-center">
        <img src="/uploads/<%-data.file%>" alt="No user image" height="200px" class="profile-preview" />
      </div>

      <!-- Submit Button -->
      <button type="submit" class="btn btn-primary mt-3">Update Profile</button>
    </form>
  </div>

  <!-- Bootstrap JS Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
