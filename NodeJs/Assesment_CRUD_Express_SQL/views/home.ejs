<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Registration</title>
    <%- include('cdn.ejs') %>
    <style>
        body {
            background-color: #121212;
            color: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .card {
            background-color: #1e1e1e;
            border: none;
            box-shadow: 0 0 15px rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
        }
        .card:hover {
            box-shadow: 0 0 25px rgba(255, 255, 255, 0.2);
        }
        .form-control {
            background-color: #2a2a2a;
            border: 1px solid #444;
            color: #fff;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }
        .form-control:focus {
            border-color: #6c63ff;
            box-shadow: 0 0 5px #6c63ff;
            background-color: #2a2a2a;
            color: #fff;
        }
        .btn-dark:hover {
            background-color: #444;
            color: #fff;
        }
    </style>
</head>
<body>

    <div class="text-center mt-5">
       <a href="/userdata"><button class="btn btn-dark px-4 fw-bold">View All Users</button> </a> 
    </div>

    <div class="container d-flex justify-content-center align-items-start mt-5">
        <div class="card p-4 col-md-6">
            <h2 class="text-center mb-4 text-light">User Registration</h2>
            <hr class="bg-light">
            <form action="/saveform" method="POST">
                <div class="mb-3">
                    <label for="userName" class="form-label text-light">Enter Employee Name:</label>
                    <input type="text" id="userName" name="userName" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label for="userEmail" class="form-label text-light">Enter Employee Email:</label>
                    <input type="email" id="userEmail" name="userEmail" class="form-control" required>
                </div>
                <div class="mb-4">
                    <label for="userPass" class="form-label text-light">Enter Employee Password:</label>
                    <input type="password" id="userPass" name="userPass" class="form-control" required>
                </div>
                <div class="text-center">
                    <button class="btn btn-dark px-4 fw-bold">Save Employee</button>
                </div>
            </form>
        </div>
    </div>

</body>

</html>
